{
  "name": "injected-web",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "check": "node --check src/prelude.js"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "jsdom": "^25.0.0"
  }
}
