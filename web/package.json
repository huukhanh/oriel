{
  "name": "injected-web",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "check": "node --check src/prelude.js",
    "test:webkit": "vitest run --config vitest.webkit.config.js",
    "test:all": "pnpm test && pnpm test:webkit"
  },
  "devDependencies": {
    "jsdom": "^25.0.0",
    "playwright": "^1.62.1",
    "vitest": "^2.0.0"
  }
}
